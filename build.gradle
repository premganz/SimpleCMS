apply plugin: 'eclipse'
apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    runtime 'org.codehaus.groovy:groovy-all:2.3.7'
	compile 'org.codehaus.groovy:groovy-all:2.3.7'
    testCompile 'junit:junit:4.11'
	compile 'org.apache.activemq:activemq-all:5.4.2'
	compile 'joda-time:joda-time:2.6'
	runtime 'joda-time:joda-time:2.6'
}

tasks.withType(JavaCompile) {
	options.fork = true
	options.forkOptions.executable = "C:/Program Files/Java/jdk1.7.0_25/bin/javac"
}

tasks.withType(GroovyCompile) {
	options.fork = true
	options.forkOptions.executable = "C:/Program Files/Java/jdk1.7.0_25/bin/javac"
}

task vh(type: JavaExec) {
	main = "org.trs.itf.handler.MainProxy"
	classpath = sourceSets.main.runtimeClasspath
}